# Production bridge handoff

Base: PR324 abc6de68ec79a2d0438dc92916b7efe30253f402. Includes the coordinator's streaming correction 2cd9d6deb3b4c7ea37aba362554b7d6bb82d7cf5 as cherry-pick 97ac800. The historical fetch signal now remains valid through response body consumption.

runRelationshipCatalogScheduler invokes the actual CTO catalog CLI and host through its explicit relationshipPipelineFactory. The paired CTO change is based on PR198 3b2cdeaf1581b62665938723a1b75ae3ea3a400a. Production must bind the real createSubscriptionCandidateReview, reviewOptionsForRun, authorized operation store, signed registry and paged recall factories. The wrapper is a callable entry point, not an installed scheduled job. Provider and authoritative registry follow-ups remain lead CTO integration dependencies.

Completed extractor operations are loaded with immutable result and source-binding checks. Candidate arrays are capped before cloning, and accumulated encoded candidates are limited to 1 MiB. A create-only local intent prevents repeated review after uncertain outcomes. The exact reviewed artifact reference is persisted and fsynced before publication POST. Lost publication acknowledgments retry that same artifact; intent-only outcomes remain held and never blindly rerun a model.

The local outbox uses an exclusive, trusted persistent directory, fixed hash paths, bounded record sizes, create-only hard links and file fsync. It stores metadata references only. Recovery reads state in reverse publication order to avoid false corruption during valid concurrent writes. Temporary files exist only at leaves and do not enter the prefix scan. This is process-restart persistence on a filesystem supporting those primitives, not a distributed queue or a guarantee against disk loss. Provision and test a persistent host volume before activation.

Recovery scans bounded pages and reports page-scoped age/count metrics through the required onMonitor callback. Watch and explicit recover modes advance the in-memory cursor within the runtime deadline. Restart begins a new scan; once may finish a page without finishing the full scan. There is no persisted cursor or whole-corpus health/completeness claim. Recall retains the existing page/cursor boundary and exact artifact contents, including source-attributed record classifications supplied by the review adapter.

Synthetic integration uses the actual scheduler CLI/host, completed extractor result loader, PR198 semantic adapter, signed identity registry verification, operation store, publication route and paged historical retrieval. Transport, extraction result input and model subprocess output are synthetic. Lost POST response recovery reuses the same artifact with no repeated extraction or review. Three documents produce a combined historical path. This does not prove live extraction, live model behavior, production identity registry authority or deployment health.

Validation: 60 gateway relationship tests and 14 client tests passed, zero skips; TypeScript compilation passed. Actual adapter tests were enabled with RELATIONSHIP_REVIEW_ROOT and RELATIONSHIP_STORE_MODULE pointing to the paired CTO checkout. The paired CTO graph suite passed 399 tests. Outputs are bridge-test-output.txt and bridge-client-test-output.txt. CI without the external CTO checkout explicitly skips the cross-repository integration test.

No deployment, policy activation, IAM mutation, real model calls or registry provisioning was performed. PR316 and CTO PR190 release holds remain unchanged. Lead CTO owns final integration and production verification.
