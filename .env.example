# OTCHealth MCP Server - Environment Variables Template
#
# This is the public template, committed to git for reference.
# Real values for this deployment are in .env (gitignored, never pushed).
# To set up a new environment: copy .env.example to .env and populate
# with values from Matt's Notion Token Vault.

# ===== Customer.io credentials (workspace 193366, US region) =====
# Source: https://fly.customer.io/settings/api_credentials
CIO_SITE_ID=
CIO_TRACK_KEY=
CIO_APP_API_BEARER=

# ===== MCP server auth =====
# Generate via: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PERPLEXITY_CONNECTOR_TOKEN=
ADMIN_REVOKE_TOKEN=

# ===== n8n integration =====
N8N_BASE_URL=https://otchealth.app.n8n.cloud
N8N_API_KEY=
N8N_WEBHOOK_SECRET=

# ===== Feature flags (defaults match ADR-001 Section 7) =====
READ_ONLY_MODE=true
ENABLE_WRITE_TOOLS=false
ENABLE_HIGH_RISK_TOOLS=false
DRY_RUN_DEFAULT=true

# ===== Server config =====
PORT=8080
NODE_ENV=production
LOG_LEVEL=info
