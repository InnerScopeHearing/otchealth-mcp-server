# Dedicated WeFunder source seat

Status: local review patch only. No credential, runtime assignment, deployment or source export is created by this change.

The `wefunder-campaign-director` principal may address only Hyperagent source agent `cmsozxle705pz07adrh3k9l2v`. It must also have that exact source in `HYPERAGENT_LANE_AGENTS` and the source must be explicitly classified `exec` in `HYPERAGENT_AGENT_CLASSES`. A missing assignment, missing classification, general classification, personal-legal classification or any other source is refused. The specialist is not added to either privileged ring. CTO remains denied the source, including ID-only reads and an accidental general classification.

The new `CODEX_WEFUNDER_MCP_TOKEN` environment slot maps a distinct static bearer to this principal and uses existing durable revocation checks. Its value must be securely provisioned and bound through AWS SSM, never placed in source, review artifacts, chat or logs. Existing confidential OAuth clients mapped to this same principal also receive its scoped behavior. Therefore an unset new static token alone does not make a deployed patch inactive; activation review must inspect existing identity metadata and source assignments without exposing credentials.

The principal receives the existing open-read baseline except `gateway_fetch_result`, plus catalog diagnostics, scoped Hyperagent listing/reading/create/send, and the existing public-only browser broker tools. This fixed allowlist applies to every authentication path. Wake cannot target another agent. Large responses remain inline because the shared result cache does not bind payloads to a caller. Hyperagent write prompts are projected to identifier and message-length metadata for both start logs and shared episode journaling.

Create and send can invoke only the exact original source agent. The existing per-lane invocation counter still applies to both. It is an in-memory per-replica runaway-loop guard, not a global spend quota or an export-only semantic policy. Messages trigger the original agent under its own permissions, so the operator must keep migration prompts scoped to private export and preserve its human approval boundaries.

No private finance/legal Brain room, shared-memory writing, source approval resolution, PHI access, browser sign-in, draft editor write or campaign publication is added. Private source assets and continuity records must remain in the separately authorized WeFunder project or an explicitly provisioned private store. The existing browser enrollment remains `public_read` only.

Before activation: obtain the exact owner-approved source entitlement, review this patch independently, run CI, deploy through the normal gateway process, securely provision the dedicated identity and project configuration, and verify live positive and negative access from their actual principals. Do not borrow another seat's credential to run acceptance. Confirm wake, catalog, source listing, both seed-thread reads, source-owned message dispatch and downstream export artifacts. Then reconcile every source asset against its export manifest before claiming migration completeness.

Synthetic validation covers exact-source access, unchanged CTO denial, other WeFunder/investor/finance/legal/general/unknown source denial, ambiguous/missing/mismatched thread owners, list filtering, shared create/send budget, dry runs, static bearer identity and revocation readiness, finance/legal Brain exclusion, legacy-auth curation, foreign wake refusal before IO, and no unscoped offload or prompt leakage to logs/episode text. Windows review uses compiled JavaScript with a clean environment and isolated runtime/cache directories because the sandbox's tsx loader cannot resolve OS user information.
