{
  "name": "OTCHealth Chat Action Result",
  "active": false,
  "settings": { "executionOrder": "v1", "saveExecutionProgress": true },
  "nodes": [
    { "id": "webhook", "name": "Chat Action Result", "type": "n8n-nodes-base.webhook", "typeVersion": 2, "position": [240, 300], "webhookId": "chat-action-result", "parameters": { "httpMethod": "POST", "path": "chat-action-result", "responseMode": "responseNode", "options": { "rawBody": true } } },
    { "id": "verify", "name": "Verify HMAC", "type": "n8n-nodes-base.code", "typeVersion": 2, "position": [460, 300], "parameters": { "language": "javaScript", "jsCode": "const crypto = require('crypto'); const secret = $env.N8N_WEBHOOK_SECRET; if (!secret) throw new Error('N8N_WEBHOOK_SECRET missing'); const h = $json.headers || {}; const raw = $binary?.data?.data ? Buffer.from($binary.data.data, 'base64').toString('utf8') : JSON.stringify($json.body || {}); const expected = crypto.createHmac('sha256', secret).update(raw, 'utf8').digest('hex'); const body = $json.body || JSON.parse(raw); return [{ json: { authorized: Boolean((h['x-signature-sha256'] || h['X-Signature-Sha256']) === expected && typeof body.job_id === 'string' && body.job_id.length > 0), body } }];" } },
    { "id": "get", "name": "Read Opaque Job Result", "type": "n8n-nodes-base.httpRequest", "typeVersion": 4, "position": [700, 220], "parameters": { "method": "POST", "url": "={{ $env.N8N_GATEWAY_BASE_URL + '/internal/chat-actions/jobs/result' }}", "authentication": "genericCredentialType", "genericAuthType": "httpHeaderAuth", "sendBody": true, "specifyBody": "json", "jsonBody": "={{ JSON.stringify({ job_id: $('Verify HMAC').first().json.body.job_id }) }}" }, "credentials": { "httpHeaderAuth": { "name": "OTCHealth Gateway Service" } } },
    { "id": "respond", "name": "Respond", "type": "n8n-nodes-base.respondToWebhook", "typeVersion": 1, "position": [940, 220], "parameters": { "respondWith": "json", "responseBody": "={{ JSON.stringify({ success: true, result: $json }) }}" } },
    { "id": "reject", "name": "Reject", "type": "n8n-nodes-base.respondToWebhook", "typeVersion": 1, "position": [700, 430], "parameters": { "respondWith": "json", "responseCode": 401, "responseBody": "={{ JSON.stringify({ success: false, error: 'invalid_signature_or_job_id' }) }}" } }
  ],
  "connections": { "Chat Action Result": { "main": [[{ "node": "Verify HMAC", "type": "main", "index": 0 }]] }, "Verify HMAC": { "main": [[{ "node": "Read Opaque Job Result", "type": "main", "index": 0 }], [{ "node": "Reject", "type": "main", "index": 0 }]] }, "Read Opaque Job Result": { "main": [[{ "node": "Respond", "type": "main", "index": 0 }]] } },
  "_otchealth_mcp_metadata": { "required_gateway_credential": "OTCHealth Gateway Service", "required_env": ["N8N_WEBHOOK_SECRET", "N8N_GATEWAY_BASE_URL"], "import_instructions": "Import inactive, bind OTCHealth Gateway Service on Read Opaque Job Result, set the two n8n environment variables, test with a synthetic opaque job id, then activate." }
}
