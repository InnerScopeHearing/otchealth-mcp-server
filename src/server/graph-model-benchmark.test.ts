import test from 'node:test';import assert from 'node:assert/strict';import {scoreGraphRun,qualityGate,benchmarkManifest} from './graph-model-benchmark.js';
const assertions=[{id:'a',subject:'x',predicate:'owns',object:'y',citations:['c1']}];
test('scores independent verifier dimensions and rejects false causation',()=>{const good=scoreGraphRun(assertions,{provider:'subscription',model:'test',elapsed_ms:1,attempts:1,candidates:[{subject:'x',predicate:'owns',object:'y',citations:['c1']}]});assert.equal(qualityGate(good),true);const bad=scoreGraphRun(assertions,{provider:'subscription',model:'test',elapsed_ms:1,attempts:1,candidates:[{subject:'x',predicate:'causes',object:'y',citations:['bad'],causation:true}]});assert.equal(bad.false_merges,1);assert.equal(bad.unsupported_causation,0);assert.equal(qualityGate(bad),false);assert.equal(benchmarkManifest(assertions,[{provider:'p',model:'m',elapsed_ms:1,attempts:1,candidates:[]}]).schema,'graph-model-benchmark-v1');});
